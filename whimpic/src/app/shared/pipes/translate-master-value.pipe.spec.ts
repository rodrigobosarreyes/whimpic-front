import { TranslateMasterValuePipe } from './translate-master-value.pipe';

describe('TranslateMasterValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateMasterValuePipe();
    expect(pipe).toBeTruthy();
  });
});
